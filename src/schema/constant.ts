export enum FormType {
    Text = 'FormText',
    Digit = 'FormDigit',
    Radio = 'FormRadio',
    Switch = 'FormSwitch',
    Block = 'FormBlock',
}

/**
 * Eq,
 *
 * TODO: maybe Some, Map, Find
 */
export enum OpType {
    Eq = 1,
}

export enum ExprValueType {
    Path = 1,
    Constant,
}
